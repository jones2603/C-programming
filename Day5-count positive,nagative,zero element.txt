count positive,nagative,zero elements:

#include <stdio.h>

int main() {
    int n;
    int positive = 0, negative = 0, zero = 0;

    printf("Enter the number of elements in the array: ");
    scanf("%d", &n);

    int arr[n];
    printf("Enter %d elements:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);

        if (arr[i] > 0)
            positive++;
        else if (arr[i] < 0)
            negative++;
        else
            zero++;
    }

    printf("\nResults:\n");
    printf("Positive numbers: %d\n", positive);
    printf("Negative numbers: %d\n", negative);
    printf("Zeroes: %d\n", zero);

    return 0;
}

Output:

Enter the number of elements in the array: 6
Enter 6 elements:
65 -23 17 0 88 72

Results:
Positive numbers: 4
Negative numbers: 1
Zeroes: 1


=== Code Execution Successful ===